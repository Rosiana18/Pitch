package myTest.util;

import java.io.IOException;

import myTest.dao.PitchDAO;

public class DataLoader {
  /*  public static void loadData(String email)
            throws IOException {
    	PitchDAO.INSTANCE.add(email);
	}
	
	public static void removeData(String email)
            throws IOException {
		PitchDAO.INSTANCE.remove(email);
	}
	public static boolean inDatabase(String email)  {
		return PitchDAO.INSTANCE.inDatabase(email);
	}
	*/
}